name: hello-world
on: workflow_dispatch
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: create
        run: |
          echo "Firmware binaries"
          echo "tools packages"
      - name: Build
        run: |
          echo "hardware tools"
      - name: Release
        run: |
          echo "Firmware AI chips steps"
          echo "AMD design"
  KWS-scan:
    needs: [my-job]
    runs-on: ubuntu-latest
    steps:
      - name: keyword_scan tools
        run: |
          echo "full repo scan including case senstive also"

  AI-scan:
    needs: [my-job, KWS-scan]
    runs-on: ubuntu-latest
    steps:
      - name: AI_platform scan
        run: |
          echo "LLM will scan this whole repo"